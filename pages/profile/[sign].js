import React from "react"
import {PersonData, AddProject, ProjectBox} from '../../components/UI/Profile'

const Sign = (props)=>{
    return(
        <>
            <PersonData/>
            <AddProject/>
            <ProjectBox/>
        </>
    )
}

export default Sign